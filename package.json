{
  "name": "the-moi-cinemas",
  "version": "1.0.0",
  "private": true,
  "description": "Cartelera con base de datos local para THE MOI CINEMAS",
  "scripts": {
    "server": "json-server db/db.json --port 3000"
  },
  "devDependencies": {
    "json-server": "1.0.0-beta.3"
  }
}
