{
  "name": "the-moi-cinemas",
  "version": "1.0.0",
  "private": true,
  "description": "Cartelera y API local de THE MOI CINEMAS",
  "scripts": {
    "start": "node scripts/start-project.js",
    "server": "json-server db/db.json --port 3000",
    "check": "node --check js/app.js && node scripts/validate-data.js",
    "test": "npm run check"
  },
  "devDependencies": {
    "json-server": "1.0.0-beta.3"
  }
}
